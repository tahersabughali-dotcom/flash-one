# Flash One environment template
#
# No backend variables are required yet.
# Database SQL lives in supabase/migrations/ but is not connected.
# Add variable NAMES here only when application code actually reads them.
#
# Copy to .env.local for local overrides.
# Never commit real values, tokens, or connection strings.
