# Flash One environment template
#
# Names only. Never put real values in this file.
# Copy to .env.local for local overrides (gitignored).
#
# Server-only. Do not prefix these with NEXT_PUBLIC_.
# Do not add service-role or database password variables until a later
# phase actually requires a privileged server path.

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
